import { Security } from '@rentling/fr-shared';
import { ProfileLayout } from '../../layout/ProfileLayout';

export default function SecurityPage() {
  return (
   <Security />
  );
}

SecurityPage.PageLayout = ProfileLayout;
